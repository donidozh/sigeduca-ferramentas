# GPE — Gestão de Pessoas

Pasta reservada para as ferramentas de Gestão de Pessoas do SigEduca (`/grh`). Ainda não há ferramentas publicadas.

Ao criar uma ferramenta aqui, use `modulos: ["grh"]` e `arquivo: "gpe/nome.user.js"` no catálogo. O menu fica vermelho automaticamente.
